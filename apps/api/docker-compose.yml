version: "3.9"

services:
  db:
    image: postgres:15
    container_name: postgres_pnpm
    restart: always
    environment:
      POSTGRES_DB: monorepo
      POSTGRES_USER: user
      POSTGRES_PASSWORD: secret
    ports:
      - "5437:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      
  
volumes:
  pgdata: