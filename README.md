

### pnpm-workspace.yaml 
-Esse arquivo é o que diz ao pnpm onde procurar os "pacotes" do workspace:

### Itens meta
-Multitenancy single database 
-auth 
-feature flag (simples)
- Integração com stripe